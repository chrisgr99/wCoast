{
  "name": "libresynth",
  "version": "0.1.0",
  "description": "LibreSynth - an open modular synthesizer for Web Audio, native Mac build via Electron.",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^42.1.0"
  }
}
